/* machine/trap.h -- FIRST-PARTY stub for the NetBSD/m68k FPE.
 * Reached from NetBSD's <m68k/signal.h>; carries the NetBSD T_* trap-code constants, none of
 * which the emulator uses.  AMIX's trap codes are u_trap's own and live in sys/fault.h.
 */
#ifndef _AMIX_FPE_MACHINE_TRAP_H_
#define _AMIX_FPE_MACHINE_TRAP_H_
#endif
