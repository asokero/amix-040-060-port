# CLAUDE.md

The guidance for working in this repository is in [`AGENTS.md`](AGENTS.md). **Read it before
changing anything.**

It is one file for every assistant, rather than one per tool, so that two sets of instructions
cannot drift apart and start contradicting each other — which is a failure this repository
documents elsewhere and would rather not demonstrate in its own root directory.
